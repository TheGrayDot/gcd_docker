version: '3'

services:

  mysql:
    image: mysql:8.0
    container_name: tgd_mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      - MYSQL_HOST=localhost
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=tgd
      - MYSQL_PORT=${MYSQL_PORT}
    volumes:
      - ./data/volumes/tgd_mysql_${GCD_DUMP_DATE_CURR}:/var/lib/mysql
      - ./data/tgd/:/docker-entrypoint-initdb.d
    networks:
      - backend

  python:
    image: python:3.9-slim-bullseye
    container_name: tgd_python
    build:
      context: .
      dockerfile: tgd/python/Dockerfile
    depends_on:
      - mysql
    restart: unless-stopped
    environment:
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=tgd
      - MYSQL_PORT=${MYSQL_PORT}
    volumes:
      - ./python:/python
    networks:
      - frontend
      - backend

networks:
  frontend:
  backend:
